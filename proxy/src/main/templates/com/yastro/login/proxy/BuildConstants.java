package com.yastro.login.proxy;

/**
 * Gerado pelo Gradle (task {@code generateTemplates}) a partir da versão do build.
 * Fonte ÚNICA da versão do plugin: {@code build.gradle} -> aqui -> {@code @Plugin}.
 * NÃO editar à mão (o arquivo real vive em build/generated, este é o template).
 */
public final class BuildConstants {

    public static final String VERSION = "${version}";

    private BuildConstants() {
    }
}
